package com.test;

public class test01 {
	public static void main(String[] args) {
		int score; // 변수 선언
		score = 95;
		score = 90; // 변수 할당, 값 변경
		System.out.println(score + " 타입 : " + ((Object)score).getClass().getName());
		int myScore = 100; // 변수 선언 + 초기화
		int age, height, weight
		
		
	}
}
